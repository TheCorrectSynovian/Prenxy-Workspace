# VIEW PRENXY OPENSOURCE DOCSX 

Web app for the Prenxy study hub with:
- Drive, Chat, and Social apps
- Mobile and desktop HTML pages
- Admin panel with terminal and system tools
- Sandbox command execution for admin workflows

## Run

```bash
npm install
npm start
```

Server starts on `http://localhost:3000` by default.
